name: atelier_valois
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_nav_bar: ^5.0.6
  provider: ^6.1.1 # <-- Added a valid version

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - lib/image/Emboried Tee.png
    - lib/image/Flame Pattern Football Jersey.png
    - lib/image/icon.png
    - lib/image/Milo Shark Tee.png
    - lib/image/Separate Shark Tee.png
